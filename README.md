# CinemaBook - Онлайн-бронирование билетов в кино

Production-ready приложение для бронирования билетов в кинотеатры, построенное на Nuxt 3.

## 🚀 Технологический стек

- **Nuxt 3** - Vue.js фреймворк с SSR
- **Nuxt UI** - UI библиотека (Tailwind CSS + Headless UI)
- **TanStack Query** - Управление серверным состоянием с кэшированием
- **Zod** - Runtime валидация данных
- **TypeScript** - Строгая типизация
- **Vitest** - Unit и component тестирование

## 🏗️ Архитектура

Проект использует **Feature-Sliced Design** для организации кода:

```
app/
├── pages/           # Маршрутизация
├── layouts/         # Layout компоненты
├── features/        # Бизнес-логика по фичам
│   ├── auth/        # Авторизация
│   ├── movies/      # Фильмы
│   ├── cinemas/     # Кинотеатры
│   ├── sessions/    # Сеансы и бронирование
│   └── bookings/    # Мои билеты
├── shared/          # Переиспользуемый код
│   ├── api/         # API клиент и репозитории
│   ├── schemas/     # Zod схемы
│   └── lib/         # Утилиты
├── composables/     # Глобальные composables
├── middleware/      # Route middleware
└── plugins/         # Nuxt плагины
```

## ✨ Ключевые особенности

### Производительность
- **TanStack Query** с stale-while-revalidate для мгновенного UI
- **Optimistic updates** для мгновенной обратной связи
- **Lazy loading** изображений
- **SSR** для быстрой первой загрузки

### Надёжность
- **Zod validation** для runtime проверки API ответов
- **AbortController** для отмены запросов
- **Error Boundary** (error.vue) для глобальной обработки ошибок
- **Retry с exponential backoff** для критических операций

### Доступность (A11y)
- Семантическая HTML разметка
- Полная поддержка клавиатурной навигации
- ARIA атрибуты для screen readers
- Skip links для быстрой навигации
- Focus management

### UX
- Skeleton loaders для загрузки
- Toast уведомления
- Пустые состояния
- Таймер оплаты с обратным отсчётом

## 🛠️ Установка и запуск

### Требования
- Node.js 18+
- npm

### Установка зависимостей

```bash
npm install
```

### Запуск frontend

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:3000`

## 📝 Скрипты

```bash
npm run dev          # Запуск dev сервера
npm run build        # Production сборка
npm run preview      # Превью production сборки
npm run lint         # Проверка кода
npm run typecheck    # Проверка типов
npm run test         # Запуск тестов в watch режиме
npm run test:run     # Одноразовый запуск тестов
npm run test:coverage # Тесты с покрытием
```

## 🧪 Тестирование

```bash
npm run test:run
```

Тесты включают:
- Unit тесты форматтеров и утилит
- Тесты Zod схем
- Тесты composables (useCountdown)

## 🔐 Аутентификация

- JWT токен хранится в cookie (SSR-safe)
- Валидация: логин/пароль минимум 8 символов
- Пароль при регистрации: минимум 1 заглавная буква и 1 цифра

## 📱 Страницы

| Путь | Описание |
|------|----------|
| `/movies` | Список фильмов |
| `/movies/:id` | Детали фильма с сеансами |
| `/cinemas` | Список кинотеатров |
| `/cinemas/:id` | Детали кинотеатра с сеансами |
| `/sessions/:id` | Выбор мест и бронирование |
| `/my-tickets` | Мои билеты (требует авторизации) |
| `/login` | Вход |
| `/register` | Регистрация |

## 🎨 Дизайн

- Светлая тема по умолчанию
- Индиго как primary цвет
- Адаптивный дизайн
- Компоненты Nuxt UI

## 📄 API

Документация API доступна по адресу: `http://localhost:3022/api-docs`

## 📁 Переменные окружения

```env
NUXT_PUBLIC_API_BASE=http://localhost:3022
```

## 📝 Лицензия

MIT
